# =============================================================================
# ClinicAI — Master Requirements (12-Agent Ecosystem)
# =============================================================================

# ── Web Framework & Servers ───────────────────────────────────────────────────
fastapi==0.115.12              # High-performance async API framework
uvicorn[standard]==0.34.3      # ASGI server for FastAPI
python-multipart==0.0.20       # Form data parsing
pydantic==2.11.5               # Input validation and schema enforcement

# ── Local AI & NLP Engine ─────────────────────────────────────────────────────
torch==2.6.0                   # PyTorch for local inference
transformers==4.49.0           # HuggingFace Transformers
accelerate==1.4.0              # Model acceleration
sentence-transformers==4.1.0   # Text embeddings for RAG
chromadb==1.0.9                # Local Vector Database for medical context

# ── Data Processing & ML ──────────────────────────────────────────────────────
pandas==3.0.1                  # Population Analytics & Batch processing
numpy==2.4.2                   # Risk scoring math
scikit-learn==1.8.0            # Predictive modeling (Agent 04)

# ── External & Network Utilities ──────────────────────────────────────────────
requests==2.32.5               # Sync HTTP (Medical APIs)
httpx==0.28.1                  # Async HTTP (Inter-agent communication)
python-dotenv==1.2.1           # Config management

# ── Security & Privacy ────────────────────────────────────────────────────────
python-jose[cryptography]==3.5.0  # JWT for Security Agent RBAC
passlib[bcrypt]==1.7.4            # Hashing

# ── Optional: Notification & HL7 (Used in Agents 06, 10, 12) ──────────────────
twilio==9.5.2                  # SMS / Voice
firebase-admin==6.9.0          # Push Notifications
sendgrid==6.12.0               # Professional Email
fhir.resources==7.1.0          # HL7 FHIR R4 models
hl7apy==1.3.4                  # Legacy HL7 support